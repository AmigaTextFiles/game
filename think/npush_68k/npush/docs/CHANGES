* Changed title screen logo to be pure 7bit ASCII since it created problems 
  on some terminals
* Added 7 new levels
* Moved levels to a separate subdirectory to avoid clutter
* Fixed to make it build on Mac OS X
* Option to configure keyboard controls is now implemented
* Added a .desktop file for application shortcut in menus
* Created a nice PNG icon for the program
* Included in Fedora 8 and 9 - just 'yum install' it
