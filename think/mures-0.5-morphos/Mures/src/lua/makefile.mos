AR			=	ar
CC			=	gcc
RANLIB	=	ranlib
RM			=	rm -f


CFLAGS	=	-g -noixemul -O2 -Wall


SRC		=	$(wildcard *.c)
OBJ		=	$(SRC:.c=.o)
BIN		=	liblua.a


all:	$(BIN)

$(BIN):	$(OBJ)
		$(AR) cru $(BIN) $(OBJ)
		$(RANLIB) $(BIN)

%.o:	%.c
		$(CC) $(CFLAGS) -c $< -o $@

clean:
		$(RM) $(OBJ) $(BIN)
