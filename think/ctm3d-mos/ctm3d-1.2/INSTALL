
ColorTileMatch 3D requires the following libraries:
	- SDL 1.2
	- OpenGL
	- GLU

On Linux, typing the following should suffice:
	make CFLAGS="-lGL -lGLU"

On Mac OS X, if typing "make" fails, this should work:
	make CFLAGS="-DMACBUILD"

On Windows/MinGW, run "mingw-build.bat".

