DEFINITION MODULE SkakKey; (* NIC and ECO keys, Windows *)

(*$ LongAlign:=TRUE StackParms:=FALSE CStrings:=TRUE LargeVars:=FALSE *)

FROM QISupport IMPORT
  VINDUE;
FROM SkakBase IMPORT
  STILLINGTYPE;

CONST
  SkakKeyDefCompilation="4";

VAR
  setWIN  :VINDUE;  (*teoWIN is in SkakFil.def *)
  SetFirst:BOOLEAN; (* TRUEs until initiated *)
  TeoFirst:BOOLEAN;

  Dominans,Select,Mund :STILLINGTYPE;
  DominansOn           :BOOLEAN;

(* Looks at the first 14 moves in SkakBrain.Spil and returns two key types:
      ECO: A0-E9 (50 different)
      NIC: AL BI CK EO FR HD IG KF KG KI KP NI PU QG QI
           QO QP RE RG RL SD SI SO VG VO (25 different)
*) 
PROCEDURE GetOpeningKeys(VAR ECO,NIC:ARRAY OF CHAR);

PROCEDURE OpenSetWin;

PROCEDURE OpenTeoWin;

PROCEDURE CalcMunde;  (* and/or Dominans (only if ~MouthOff/DominansOn) *)

PROCEDURE MarkMoves(frafelt:INTEGER):INTEGER;

END SkakKey.
