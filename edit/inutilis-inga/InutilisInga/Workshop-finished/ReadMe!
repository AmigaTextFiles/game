To use the Workshop project file in SkriptShell, you have to adjust
all paths in the settings window, after loading the project. Else the
scripts cannot be compiled or edited (the text editor would create
new files, because it could not find the originals)

Um die Workshopprojektdatei in der SkriptShell benutzen zu können,
müssen die Pfade in den Einstellungen angepasst werden, nachdem
das Projekt geladen wurde. Sonst können die Skripte weder
kompiliert noch bearbeitet werden (der Texteditor würde eine neue
Datei erschaffen, da er die richtige nicht findet).