(set @default-dest
(askdir
   (prompt ("Where should %s installed ?\nA drawer \"%s\" will automatically created." @app-name @app-name))
   (help @askdir-help)
   (default @default-dest)
   (disk)
)
)

(set #dest (tackon @default-dest @app-name))

(set @rdest 
(askdir
   (prompt ("Select a path directory for RNCDepack"))
   (help @askdir-help)
   (default @default-dest)
   (disk)
)
)
(set #pippo (@rdest))
	
(makedir #dest
   (help @makedir-help)
   (infos)
)


;----------------------------

(copyfiles
   (help @copyfiles-help)
   (source "SWOSTED_3.2")
   (dest #dest)
   (infos)
)

(copyfiles
   (help @copyfiles-help)
   (source "English.guide")
   (dest #dest)
   (infos)
)

(copyfiles
   (help @copyfiles-help)
   (source "Italiano.guide")
   (dest #dest)
   (infos)
)

(copyfiles
   (help @copyfiles-help)
   (source "logo.abk")
   (dest #dest)
   (infos)
)

(copyfiles
   (help @copyfiles-help)
   (source "data.dat")
   (dest #dest)
   (infos)
)

(copyfiles
   (help @copyfiles-help)
   (source "rncdepack")
   (dest #pippo)
   (infos)
)

(copyfiles
   (help @copyfiles-help)
   (source "rncdepack.doc")
   (dest #pippo)
   (infos)
)

(message ("\n %s Written by Silvio Banzi in July 1998" @app-name))

(exit)

