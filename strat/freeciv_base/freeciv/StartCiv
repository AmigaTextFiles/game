.key SAVENAME
.bra {
.ket }

if {SAVENAME}
  if not exists {SAVENAME}
    echo "{SAVENAME} doesn't exists!"
    skip quit
  endif
endif

setenv FREECIV_DATADIR `cd`/data
newshell FROM StartClient
stack 150000

if {SAVENAME}
  civserver -f {SAVENAME}
else
  civserver
endif

lab quit
