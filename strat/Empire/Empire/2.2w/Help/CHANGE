change country <country>
    switch to another country
change name
    change this country's name (current password needed)
change password
    change this country's password (current password needed)
change notify [message|telegram|both]
    change what you want to happen for automatic messages generated by the
    Empire program, such as when people attack you. You can have the
    information sent as a direct message (only when you are logged on),
    stored as a telegram, or both.
change compressed
    Toggles the default mode for map output. By default map output is NOT
    compressed. The setting can be temporarily overrided by using the "!"
    or "compressed" option of the map command.
Doc: intro, society
Help: country, telegram, message, read, map
