/*
 * kopiert den aktuellen CR aus der Mail heraus,
 * Aufruf erfolgt immer über savecr.bat !!!!!!
 */

OPTIONS RESULTS
ADDRESS YAM

/* Folder ggf. richtig setzen */
SetFolder "Sitanleta.CR"

/* ausgelesen wird IMMER die erste Mail */
MailInfo STEM mi.
Mailread QUIET
ReadInfo STEM ri.

thismail = ri.tempfile.0

address command "c:copy "||thismail||" to aktueller.cr"
address command "kolumbus -z -c -m"
