(copylib
  (help @copylib-help)
  (prompt "Installing translator.library")
  (source "libs/translator.library")
  (dest "Libs:")
  (confirm)
)
(copylib
  (help @copylib-help)
  (prompt "Installing Devs:narrator.device")
  (source "Devs/narrator.device")
  (dest "Devs:")
  (confirm)
)
(complete 10%)
(set #destdir
  (askdir
    (prompt "Where would you like the TD3 drawer to be created")
    (help @askdir-help)
    (default "Ram:")
  )
)
(complete 20%)
(set #dest (tackon #destdir "TD3"))
(makedir #dest
   (infos)
)
(copylib
  (help @copylib-help)
  (prompt "Installing reqtools.library")
  (source "libs/reqtools.library")
  (dest "Libs:")
  (confirm)
)
(set #destf "Fonts:")
(copyfiles
  (prompt "Copying Fonts")
  (help @copyfiles-help)
  (source "fonts")
  (dest #destf)
  (all)
  (confirm)
)
(set @default-dest #dest)
(copyfiles
  (prompt "Copying all files from TD3: disk to TD3 drawer")
  (help @copyfiles-help)
  (source "")
  (dest #dest)
  (all)
  (infos)
  (confirm)
)
(set #dests (tackon #dest "Start_TD3"))
(delete #dests)
(copyfiles
  (prompt "Copying Start_TD3 script to TD3 drawer")
  (help @copyfiles-help)
  (source "s/")
  (pattern "start_TD3_HD")
  (dest #dest)
)
(set #destin (tackon #dest "Start_TD3_HD"))
(rename #destin #dests)
(complete 40%)
(copyfiles
  (prompt "Copying TD3A: files to TD3 drawer")
  (help @copyfiles-help)
  (source "")
  (dest #dest)
  (all)
  (infos)
  (confirm)
)
(complete 70%)
(set name
  (askchoice
    (help @askchoice-help)
    (prompt "Would you like your TD2 routes and locos copied into the TD3 drawer?")
    (choices "Yes" "No")
  )
)
(if (= name 0)
(
(set #srcdir
  (askdir
    (prompt "Where is your old TD2 drawer?")
    (help @askdir-help)
    (default "TD2:")
  )
)
(set #srcdir2 (tackon #srcdir "Routes"))
(set #dest2 (tackon #dest "Routes"))

(copyfiles
  (prompt "Copying TD2 Route files to TD3 drawer")
  (help @copyfiles-help)
  (source #srcdir2)
  (dest #dest2)
  (all)
  (confirm)
)
(complete 80%)
(set #srcdir2 (tackon #srcdir "Locos"))
(set #dest2 (tackon #dest "Locos"))
(copyfiles
  (prompt "Copying TD2 Loco files to TD3 drawer")
  (help @copyfiles-help)
  (source #srcdir2)
  (dest #dest2)
  (all)
  (confirm)
)
(complete 90%)
(set #srcdir3 (tackon #srcdir "Save"))
(set #dest2 (tackon #dest "Save"))
(copyfiles
  (prompt "Copying TD2 Save files to TD3 drawer")
  (help @copyfiles-help)
  (source #srcdir3)
  (dest #dest2)
  (all)
  (confirm)
)
)
)
(complete 100%)
(message  "Remove both disks before running TD3 from your hard drive")
