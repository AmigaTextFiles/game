; this file is the file that first runs TAGi (the intro), then TAGe (the
; game), the reason for this is that when debugging, you don't want to have
; to watch the bloody intro all the time! Now do you?
TAGi
TAGe
