/*
 *	Mountlist entry for MEM:
 *
 *	MEM: is a pseudo disk device that reads a specific block of memory
 *	as a file - e.g. "Type HEX MEM:F80000+80000" dumps the Kickstart ROM.
 */

    Handler = L:Mem-Handler
    Stacksize = 3000
    Priority = 5
    GlobVec = -1
#
