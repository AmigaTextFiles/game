**AmigaOS4 Changes**
* Added versioning to SDL window
* Transient assigns of ETC and USR to avoid requests caused due to soft-openal if ETC/USR are not assigned (caused due to soft-openal)

**Bug Fixes**
* Fix support for non power of two sky textures sizes.
