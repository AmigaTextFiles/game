# This is the config file for Bomns for Linux.
# In here you can specify the values for a bunch
# of different things, you'll figure it out.

# this can be either true or false, I suggest setting it to true
# if it doesn't crash your computer (which it shouldn't)
fullscreen = false

# show the framerate or... not
showfps = true

# pretty obvious, play sounds or not
sound = true

# length of each match in seconds, set to 0 for unlimited time
gamelength = 100

# these can be any integer value (within reason)
bomns = 20
walls = 900
warps = 2
powups = 50
powdowns = 10
invulnerabilities = 10
health = 10

# number of bomns each player starts with.
# (set these to 0 for an interesting match)
p1startbomns = 10
p2startbomns = 10

# this is an unsigned long, or random
# (you can also use seed = 0 for random)
seed = random
