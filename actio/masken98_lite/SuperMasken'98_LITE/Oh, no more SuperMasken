if not exists fonts:OONoMoreSuperMask.font
	assign fonts: fonts add
endif
version >nil: hisoftbasic.library 2 3
if warn
	assign libs: libs add
endif

borayletter intro.ble
SuperMasken2 >nil: hd

if exists env:Sys/palette.prefs
	SYS:Prefs/Palette env:Sys/palette.prefs use
endif


