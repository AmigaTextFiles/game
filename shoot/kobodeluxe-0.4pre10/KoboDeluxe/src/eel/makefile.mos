AR			=	ar
CC			=	gcc
RANLIB	=	ranlib
RM			=	rm -f


CFLAGS	= -g -O2 -Wall `sdl-config --cflags` -I../


SRC		=	$(wildcard *.c *.cpp)
OBJ		=	$(SRC:.c=.o)
BIN		=	libeel.a


all: $(BIN)

$(BIN):	$(OBJ)
		$(AR) cru $(BIN) $(OBJ)
		$(RANLIB) $(BIN)

%.o:		%.c
		$(CC) $(CFLAGS) -c $<

clean:
		$(RM) $(OBJ) $(BIN)
