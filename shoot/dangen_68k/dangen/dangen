assign >nil: USR: "" add
set HOME /PROGDIR
stack 150000  ; <- I think that should b enough. ;)
CPU >t:temp.cpu
search >nil: t:temp.cpu 6888
if warn
   run dangen.nofpu
else
   run dangen.fpu --full-screen
endif
delete >nil: t:temp.cpu
