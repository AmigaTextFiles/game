c/Intro_scrn

c/ppanim -o part1.anim  ; needs library files under WB 1.2/1.3
c/ppanim -o part2.anim
;Disk-Validator & Ram-Handler needed under WB 1.2/1.3

failat 110  ; Command not in rom under 1.2/1.3

; PPanim plays larger animation files than
; using the PP patch with a nornal animation player.

c/pp >nil:  ; now its Ok to use PP to decrunch data files.

high_octane

;NOTE: This version does not need the Mathtrans library anymore.

;                         - TEX(94) -
