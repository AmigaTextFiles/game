SOUNDS = crawler.wav crazy.wav def_weapon.wav pickup.wav punch.wav \
         ship.wav speedy.wav thrower.wav tickler.wav

soundsi_DATA = index $(SOUNDS)

EXTRA_DIST = index $(SOUNDS)

CLEANFILES = *~
DISTCLEANFILES = .deps/*.P
