The bin directory has executables for GNU Linux/Mac OS X (mapinfo)
and Windows (mapinfo.exe). Copy the executable to the directory
where you keep your Quake 2 map files.

From the command line, you can execute the program this way:

mapinfo -f filename.map

For additional options:

mapinfo -h