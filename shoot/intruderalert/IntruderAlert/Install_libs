If NOT Exists libs:iff.library
copy iff.library libs:
ELSE
echo Iff.library detected.
ENDIF
IF NOT Exists libs:medplayer.library
copy medplayer.library libs:
ELSE 
echo medplayer.library detected
ENDIF
