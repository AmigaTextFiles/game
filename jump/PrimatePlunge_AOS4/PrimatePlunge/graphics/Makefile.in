# Makefile.in generated by automake 1.7.6 from Makefile.am.
# @configure_input@

# Copyright 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002, 2003
# Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
top_builddir = ..

am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
INSTALL = @INSTALL@
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
target_triplet = @target@
ACLOCAL = @ACLOCAL@
AMDEP_FALSE = @AMDEP_FALSE@
AMDEP_TRUE = @AMDEP_TRUE@
AMTAR = @AMTAR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
EXEEXT = @EXEEXT@
GAMEDATADIR = @GAMEDATADIR@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LDFLAGS = @LDFLAGS@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
OBJEXT = @OBJEXT@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
SDL_CFLAGS = @SDL_CFLAGS@
SDL_CONFIG = @SDL_CONFIG@
SDL_LIBS = @SDL_LIBS@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
ac_ct_CC = @ac_ct_CC@
ac_ct_STRIP = @ac_ct_STRIP@
am__fastdepCC_FALSE = @am__fastdepCC_FALSE@
am__fastdepCC_TRUE = @am__fastdepCC_TRUE@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
datadir = @datadir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localstatedir = @localstatedir@
mandir = @mandir@
oldincludedir = @oldincludedir@
prefix = @prefix@
program_transform_name = @program_transform_name@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
sysconfdir = @sysconfdir@
target = @target@
target_alias = @target_alias@
target_cpu = @target_cpu@
target_os = @target_os@
target_vendor = @target_vendor@

graphicsdir = $(pkgdatadir)/graphics
junglejdir = $(pkgdatadir)/graphics/junglej
crumbledir = $(pkgdatadir)/graphics/crumble
frenziedir = $(pkgdatadir)/graphics/frenzie
moltenmdir = $(pkgdatadir)/graphics/moltenm
skyscradir = $(pkgdatadir)/graphics/skyscra
menuitemsdir = $(pkgdatadir)/graphics/menuitems

dist_graphics_DATA = \
	antiGrav.bmp \
	backtomain.bmp \
	branch.bmp \
	bubbles.bmp \
	ceilingSpikes.bmp \
	clock.bmp \
	conveyorleft.bmp \
	conveyorright.bmp \
	evilBranch.bmp \
	fall.bmp \
	fallLeft.bmp \
	fallRight.bmp \
	flipper.bmp \
	gameCompleteScreen.bmp \
	heal.bmp \
	healthEmpty.bmp \
	healthFull.bmp \
	idle.bmp \
	instructions.bmp \
	instructions2.bmp \
	jetpackMonkey.bmp \
	jetpackPickup.bmp \
	jumpPickup.bmp \
	laser.bmp \
	linuxThankYou.bmp \
	loading.bmp \
	locked.bmp \
	log.bmp \
	logo.bmp \
	medal.bmp \
	monkeyIdle.bmp \
	need.bmp \
	needComplete.bmp \
	next.bmp \
	numbers.bmp \
	off.bmp \
	on.bmp \
	paraBag.bmp \
	parachute.bmp \
	paused.bmp \
	pipe.bmp \
	piranha.bmp \
	prev.bmp \
	primateplunged.bmp \
	record.bmp \
	rustSpikes.bmp \
	shield.bmp \
	silver.bmp \
	spikeRock.bmp \
	springRope.bmp \
	star.bmp \
	starRating1Screen.bmp \
	starRating2Screen.bmp \
	starRating3Screen.bmp \
	starRating4Screen.bmp \
	starRating5Screen.bmp \
	swingLeft.bmp \
	swingRight.bmp \
	taskBarIcon.bmp \
	whale.bmp \
	worldunlocked.bmp


dist_junglej_DATA = \
	junglej/backTrees.bmp \
	junglej/background.bmp \
	junglej/frame.bmp \
	junglej/sideTrunks.bmp \
	junglej/vines.bmp


dist_crumble_DATA = \
	crumble/frame.bmp \
	crumble/largeBackground.bmp \
	crumble/medRocks.bmp \
	crumble/nearRocks.bmp


dist_frenzie_DATA = \
	frenzie/background.bmp \
	frenzie/cliff.bmp \
	frenzie/frame.bmp \
	frenzie/waterBack.bmp \
	frenzie/waterFront.bmp


dist_moltenm_DATA = \
	moltenm/background.bmp \
	moltenm/distant.bmp \
	moltenm/frame.bmp \
	moltenm/hotRocks.bmp \
	moltenm/middleRocks.bmp \
	moltenm/normal.bmp \
	moltenm/rockFallApart.bmp \
	moltenm/sides.bmp \
	moltenm/veryHotPlatform.bmp


dist_skyscra_DATA = \
	skyscra/background.bmp \
	skyscra/frame.bmp \
	skyscra/pipesBack.bmp \
	skyscra/pipesFore.bmp \
	skyscra/pipesMid.bmp \
	skyscra/rain.bmp


dist_menuitems_DATA = \
	menuitems/back-menuitem.bmp \
	menuitems/back-over-menuitem.bmp \
	menuitems/cancel-menuitem.bmp \
	menuitems/cancel-over-menuitem.bmp \
	menuitems/instructions-menuitem.bmp \
	menuitems/instructions-over-menuitem.bmp \
	menuitems/music-menuitem.bmp \
	menuitems/music-over-menuitem.bmp \
	menuitems/newgame-menuitem.bmp \
	menuitems/newgame-over-menuitem.bmp \
	menuitems/options-menuitem.bmp \
	menuitems/options-over-menuitem.bmp \
	menuitems/quit-menuitem.bmp \
	menuitems/quit-over-menuitem.bmp \
	menuitems/reset-menuitem.bmp \
	menuitems/reset-over-menuitem.bmp \
	menuitems/sound-menuitem.bmp \
	menuitems/sound-over-menuitem.bmp \
	menuitems/sure-reset.bmp \
	menuitems/yes-menuitem.bmp \
	menuitems/yes-over-menuitem.bmp

subdir = graphics
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
CONFIG_HEADER = $(top_builddir)/src/config.h
CONFIG_CLEAN_FILES =
DIST_SOURCES =
DATA = $(dist_crumble_DATA) $(dist_frenzie_DATA) $(dist_graphics_DATA) \
	$(dist_junglej_DATA) $(dist_menuitems_DATA) \
	$(dist_moltenm_DATA) $(dist_skyscra_DATA)

DIST_COMMON = $(dist_crumble_DATA) $(dist_frenzie_DATA) \
	$(dist_graphics_DATA) $(dist_junglej_DATA) \
	$(dist_menuitems_DATA) $(dist_moltenm_DATA) \
	$(dist_skyscra_DATA) Makefile.am Makefile.in
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  Makefile.am  $(top_srcdir)/configure.ac $(ACLOCAL_M4)
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --gnu  graphics/Makefile
Makefile:  $(srcdir)/Makefile.in  $(top_builddir)/config.status
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)
uninstall-info-am:
dist_crumbleDATA_INSTALL = $(INSTALL_DATA)
install-dist_crumbleDATA: $(dist_crumble_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(crumbledir)
	@list='$(dist_crumble_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_crumbleDATA_INSTALL) $$d$$p $(DESTDIR)$(crumbledir)/$$f"; \
	  $(dist_crumbleDATA_INSTALL) $$d$$p $(DESTDIR)$(crumbledir)/$$f; \
	done

uninstall-dist_crumbleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_crumble_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(crumbledir)/$$f"; \
	  rm -f $(DESTDIR)$(crumbledir)/$$f; \
	done
dist_frenzieDATA_INSTALL = $(INSTALL_DATA)
install-dist_frenzieDATA: $(dist_frenzie_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(frenziedir)
	@list='$(dist_frenzie_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_frenzieDATA_INSTALL) $$d$$p $(DESTDIR)$(frenziedir)/$$f"; \
	  $(dist_frenzieDATA_INSTALL) $$d$$p $(DESTDIR)$(frenziedir)/$$f; \
	done

uninstall-dist_frenzieDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_frenzie_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(frenziedir)/$$f"; \
	  rm -f $(DESTDIR)$(frenziedir)/$$f; \
	done
dist_graphicsDATA_INSTALL = $(INSTALL_DATA)
install-dist_graphicsDATA: $(dist_graphics_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(graphicsdir)
	@list='$(dist_graphics_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_graphicsDATA_INSTALL) $$d$$p $(DESTDIR)$(graphicsdir)/$$f"; \
	  $(dist_graphicsDATA_INSTALL) $$d$$p $(DESTDIR)$(graphicsdir)/$$f; \
	done

uninstall-dist_graphicsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_graphics_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(graphicsdir)/$$f"; \
	  rm -f $(DESTDIR)$(graphicsdir)/$$f; \
	done
dist_junglejDATA_INSTALL = $(INSTALL_DATA)
install-dist_junglejDATA: $(dist_junglej_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(junglejdir)
	@list='$(dist_junglej_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_junglejDATA_INSTALL) $$d$$p $(DESTDIR)$(junglejdir)/$$f"; \
	  $(dist_junglejDATA_INSTALL) $$d$$p $(DESTDIR)$(junglejdir)/$$f; \
	done

uninstall-dist_junglejDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_junglej_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(junglejdir)/$$f"; \
	  rm -f $(DESTDIR)$(junglejdir)/$$f; \
	done
dist_menuitemsDATA_INSTALL = $(INSTALL_DATA)
install-dist_menuitemsDATA: $(dist_menuitems_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(menuitemsdir)
	@list='$(dist_menuitems_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_menuitemsDATA_INSTALL) $$d$$p $(DESTDIR)$(menuitemsdir)/$$f"; \
	  $(dist_menuitemsDATA_INSTALL) $$d$$p $(DESTDIR)$(menuitemsdir)/$$f; \
	done

uninstall-dist_menuitemsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_menuitems_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(menuitemsdir)/$$f"; \
	  rm -f $(DESTDIR)$(menuitemsdir)/$$f; \
	done
dist_moltenmDATA_INSTALL = $(INSTALL_DATA)
install-dist_moltenmDATA: $(dist_moltenm_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(moltenmdir)
	@list='$(dist_moltenm_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_moltenmDATA_INSTALL) $$d$$p $(DESTDIR)$(moltenmdir)/$$f"; \
	  $(dist_moltenmDATA_INSTALL) $$d$$p $(DESTDIR)$(moltenmdir)/$$f; \
	done

uninstall-dist_moltenmDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_moltenm_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(moltenmdir)/$$f"; \
	  rm -f $(DESTDIR)$(moltenmdir)/$$f; \
	done
dist_skyscraDATA_INSTALL = $(INSTALL_DATA)
install-dist_skyscraDATA: $(dist_skyscra_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(skyscradir)
	@list='$(dist_skyscra_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(dist_skyscraDATA_INSTALL) $$d$$p $(DESTDIR)$(skyscradir)/$$f"; \
	  $(dist_skyscraDATA_INSTALL) $$d$$p $(DESTDIR)$(skyscradir)/$$f; \
	done

uninstall-dist_skyscraDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_skyscra_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(skyscradir)/$$f"; \
	  rm -f $(DESTDIR)$(skyscradir)/$$f; \
	done
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:

DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)

top_distdir = ..
distdir = $(top_distdir)/$(PACKAGE)-$(VERSION)

distdir: $(DISTFILES)
	$(mkinstalldirs) $(distdir)/crumble $(distdir)/frenzie $(distdir)/junglej $(distdir)/menuitems $(distdir)/moltenm $(distdir)/skyscra
	@srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's|.|.|g'`; \
	list='$(DISTFILES)'; for file in $$list; do \
	  case $$file in \
	    $(srcdir)/*) file=`echo "$$file" | sed "s|^$$srcdirstrip/||"`;; \
	    $(top_srcdir)/*) file=`echo "$$file" | sed "s|^$$topsrcdirstrip/|$(top_builddir)/|"`;; \
	  esac; \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
	    dir="/$$dir"; \
	    $(mkinstalldirs) "$(distdir)$$dir"; \
	  else \
	    dir=''; \
	  fi; \
	  if test -d $$d/$$file; then \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)

installdirs:
	$(mkinstalldirs) $(DESTDIR)$(crumbledir) $(DESTDIR)$(frenziedir) $(DESTDIR)$(graphicsdir) $(DESTDIR)$(junglejdir) $(DESTDIR)$(menuitemsdir) $(DESTDIR)$(moltenmdir) $(DESTDIR)$(skyscradir)
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-rm -f Makefile $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am

distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

info: info-am

info-am:

install-data-am: install-dist_crumbleDATA install-dist_frenzieDATA \
	install-dist_graphicsDATA install-dist_junglejDATA \
	install-dist_menuitemsDATA install-dist_moltenmDATA \
	install-dist_skyscraDATA

install-exec-am:

install-info: install-info-am

install-man:

installcheck-am:

maintainer-clean: maintainer-clean-am

maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-dist_crumbleDATA uninstall-dist_frenzieDATA \
	uninstall-dist_graphicsDATA uninstall-dist_junglejDATA \
	uninstall-dist_menuitemsDATA uninstall-dist_moltenmDATA \
	uninstall-dist_skyscraDATA uninstall-info-am

.PHONY: all all-am check check-am clean clean-generic distclean \
	distclean-generic distdir dvi dvi-am info info-am install \
	install-am install-data install-data-am \
	install-dist_crumbleDATA install-dist_frenzieDATA \
	install-dist_graphicsDATA install-dist_junglejDATA \
	install-dist_menuitemsDATA install-dist_moltenmDATA \
	install-dist_skyscraDATA install-exec install-exec-am \
	install-info install-info-am install-man install-strip \
	installcheck installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic pdf \
	pdf-am ps ps-am uninstall uninstall-am \
	uninstall-dist_crumbleDATA uninstall-dist_frenzieDATA \
	uninstall-dist_graphicsDATA uninstall-dist_junglejDATA \
	uninstall-dist_menuitemsDATA uninstall-dist_moltenmDATA \
	uninstall-dist_skyscraDATA uninstall-info-am

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
