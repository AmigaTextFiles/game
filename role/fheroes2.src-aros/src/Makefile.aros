ifndef WITHOUT_MIXER
LIBS := $(LIBS) -lSDL -lmikmod -lvorbisfile -lvorbis -logg
endif

ifndef WITHOUT_IMAGE
LIBS := $(LIBS) -lSDL -lpng -ljpeg
endif

ifndef WITHOUT_UNICODE
CFLAGS := $(CFLAGS) -DWITH_ICONV
LIBS := $(LIBS) -lintl -lfreetype2 -liconv
endif
