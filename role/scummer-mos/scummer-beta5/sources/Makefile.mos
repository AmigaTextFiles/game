SRC = scummer.cpp graphicengine.cpp image.cpp blocks.cpp iostuff.cpp graphics.cpp viewer.cpp

OBJECTS = $(SRC:.cpp=.o)

CFLAGS= -g -Wall

all: $(OBJECTS)
	g++ `sdl-config --cflags --libs` $(OBJECTS) -o scummer -lSDL
clean:
	rm -f $(OBJECTS)
