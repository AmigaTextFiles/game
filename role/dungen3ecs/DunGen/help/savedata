   This saves your work as a data file for later work or admiration. The save
process uses a very simplistic compression scheme to shrink the size of the
files. It counts repeated types of dungeon squares and then strips them from
the file, storing only the definition and count.