IF NOT EXISTS S:PBD_HS1.bin
Copy PBD_HS1.bin S:
ENDIF

IF NOT EXISTS S:PBD_HS2.bin
Copy PBD_HS2.bin S:
ENDIF

IF NOT EXISTS S:PBD_Options.bin
Copy PBD_Options.bin S:
ENDIF

IF NOT EXISTS S:PBD_TempOptA.bin
Copy PBD_TempOptA.bin S:
ENDIF

PinBallDamage.Trainer
BrainDamage.exe
