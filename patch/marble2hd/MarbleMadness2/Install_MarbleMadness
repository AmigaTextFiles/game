; $VER: MarbleMadness HD Fix 2 Install-Script V1.0 (30.12.94) by Jean-Francois Fabre
; Adapted from an installer from Sentinel

(set GameDir "Marble")

;try to figure out a place where the user usually installs his games
(if (exists "Games:" (noreq) )
    (set @default-dest "Games:")
    (if (exists "SYS:Games" (noreq) )
        (set @default-dest "SYS:Games")
        (if (exists "Work:Games" (noreq) )
            (set @default-dest "Work:Games")
            (if (exists "JEUX:" (noreq) )
               (set @default-dest "JEUX:")
               (set @default-dest "SYS:")
            )
        )
    )
)

;ask the user to select a directory to install the game into
(set default-dest
     (tackon (askdir (prompt "Where would you like " @app-name " installed?\n"
                             "A drawer called " GameDir " will be created.")
                     (help @askdir-help)
                     (default @default-dest)
                     (newpath)
             )
     GameDir
     )
)
(set @default-dest default-dest)
(complete 5)

;create the selected directory
(makedir @default-dest
         (infos)
)
(complete 7)

(message "\n\n\n\n\n\nPlease insert Marble Madness disk in any drive"
)

(copyfiles (source "MarbleMadness!:")
           (dest @default-dest)
	   (pattern "#?")
           (files)
)

(complete 90)

;copy all extra files to this directory

(copyfiles (source "MarbleMadness!")
           (dest @default-dest)
)

(copyfiles (source "Pointer")
           (dest @default-dest)
)

(copyfiles (source "Game")
           (dest @default-dest)
)

(copyfiles (source "pointer.prefs")
           (dest @default-dest)
)

(copyfiles (source "Game.icon")
           (dest @default-dest)
	   (newname "Game.info")
)

(delete (tackon @default-dest "MarbleMadness!.info"))

(complete 100)

